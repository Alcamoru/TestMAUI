import type { FetchOptions } from '../types';
import type { Client } from '../client';
/**
 * List of managers that interact with DDragon/CDragon.
 */
export type DragonManagerKey = 'champions' | 'items' | 'runes' | 'summonerSpells';
/**
 * List of managers that interact with the API.
 */
export type ApiManagerKey = 'account' | 'challenge' | 'championMastery' | 'clash' | 'currentGame' | 'league' | 'match' | 'summoner';
/**
 * A utility for parsing the fetch options using request specific, client config and default settings.
 */
export declare const parseFetchOptions: (client: Client, manager: DragonManagerKey | ApiManagerKey, options?: FetchOptions) => FetchOptions;
//# sourceMappingURL=parseFetchOptions.d.ts.map