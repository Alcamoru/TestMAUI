import type { RateLimiterOptions } from './config';
import type { Client } from '../client';
import { AxiosResponse } from 'axios';
import type { ApiRequestOptions } from './requestOptions';
/**
 * The rate limiter class.
 * Used to send requests to the API.
 */
export declare class RateLimiter {
    private appLimit;
    private methodLimit;
    private readonly throw;
    private readonly strategy;
    private readonly client;
    private readonly _http;
    private readonly _queue;
    constructor(client: Client, options: RateLimiterOptions, apiKey: string);
    /**
     * Send a request to the API.
     * @param url - The URL to send the request to.
     * @param options - The request options.
     */
    request(url: string, options: ApiRequestOptions): Promise<AxiosResponse>;
    private requestFromApi;
    private _check;
    private _checkAppLimit;
    private _checkMethodLimit;
    private _updateFromHeaders;
    private _update;
    private _calculateSpreadDelay;
    private _fetchAppLimitUsage;
    private _fetchMethodLimitUsage;
}
//# sourceMappingURL=limiter.d.ts.map