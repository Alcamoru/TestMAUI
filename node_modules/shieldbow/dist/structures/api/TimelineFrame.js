"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimelineFrame = void 0;
const ParticipantFrame_1 = require("./ParticipantFrame");
const TimelineEvent_1 = require("./TimelineEvent");
/**
 * A representation of a frame in a match timeline.
 */
class TimelineFrame {
    /**
     * Create a new timeline frame.
     * @param data - The raw data from the API.
     * @param items - A collection of all items in the game.
     */
    constructor(data, items) {
        /**
         * The events that took place in this frame.
         */
        Object.defineProperty(this, "events", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        /**
         * The participant frames for this timeline frame.
         */
        Object.defineProperty(this, "participantFrames", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.events = data.events.map((event) => TimelineEvent_1.TimelineEventFactory.create(event, items));
        this.participantFrames = Object.values(data.participantFrames).map((frame) => new ParticipantFrame_1.ParticipantFrame(frame));
    }
}
exports.TimelineFrame = TimelineFrame;
//# sourceMappingURL=TimelineFrame.js.map